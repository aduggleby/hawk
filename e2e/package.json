{
  "name": "hawk-e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "docker compose -f ../docker-compose.e2e.yml up --build --exit-code-from e2e e2e",
    "test:pw": "playwright test",
    "test:pw:ui": "playwright test --ui",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "1.58.2",
    "typescript": "^5.7.3"
  }
}
